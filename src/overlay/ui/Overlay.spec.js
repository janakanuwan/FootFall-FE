import Overlay from './Overlay';

const overlayData = {
  title: 'Loading...',
  message: 'Verifying login ...',
  open: true,
  onClose: e => console.log(e),
}

describe('Overlay', () => {
  // FIXME
  throw ('NEED TO IMPLEMENT');
});
